import { PercentformatPipe } from "./percentformat.pipe";

describe("PercentformatPipe", () => {
  it("create an instance", () => {
    const pipe = new PercentformatPipe();
    expect(pipe).toBeTruthy();
  });
});
